# LOGSHRADDED
LOGSHREDDER is a tactical Linux log parsing engine designed for CTF forensics and incident response. Built in Python, it automates SSH brute-force discovery, parses directory traversal web vectors, and decodes base64 payload configurations while dynamically compiling iptables/ufw blocklists and structured JSON threat intelligence outputs.
